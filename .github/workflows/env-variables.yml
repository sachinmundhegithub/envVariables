name: test-env

on:
  push:
  workflow_dispatch:

jobs:
  job1:
    env:
      envVar: "job value"
    runs-on: ubuntu-latest
    
    steps:
    - name: job1-step-1
      run: echo  $envVar
  job2:
    runs-on: ubuntu-latest
    steps:
    - name: job2-step-1
      run: echo $envVar      
     
